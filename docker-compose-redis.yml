services:
  redis:
    image: redis:7.2-alpine
    container_name: redis
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    # 네트워크 정의
    networks:
      - "discodeit-network"

volumes:
  redis-data: