package com.sprint.mission.discodeit.entity;

public class Message {
	// 신규 메세지 - id(채널 UUID), pw(비공개 채널?), name(채널명), createdAt(최초 생성), updatedAt(생성 직후?, 수정 시)

	// 정보 수정 - pw, name, updatedAt

	// getter, setter
}
