package com.sprint.mission.discodeit.entity;

public class User {
	/*
	 * # User field
	 *
	 * ## Local
	 * - pw(비밀번호)
	 * - name(회원명)
	 *
	 * ## Common
	 * - id(UUID)
	 * - createdAt(최초 생성)
	 * - updatedAt(생성 직후?, 수정 시)
	 */

}
