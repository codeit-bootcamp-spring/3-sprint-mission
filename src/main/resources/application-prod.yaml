spring:
  datasource:
    url:
    driver-class-name: org.postgresql.Driver
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 80

logging:
  level:
    root: info
    com.sprint.mission: info

decorator:
  datasource:
    p6spy:
      enable-logging: false
