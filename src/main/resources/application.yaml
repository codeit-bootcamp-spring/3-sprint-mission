# server 설정
server:
  port: 8080

  # application.yaml
  #discodeit:
  #  repository:
  #    type: file   # jcf | file
  #    file-directory: .discodeit

  storage:
    type: local
    local:
      root-path: "./data" # ${user.dir}/data



# /api-docs endpoint custom path
springdoc:
  api-docs:
    # OpenAPI 명세서 경로(JSON)
    path: /api-docs
    version: openapi_3_0
  swagger-ui:
    operations-sorter: alpha


# DB config
spring:
  config:
    import: "classpath:db.yaml"