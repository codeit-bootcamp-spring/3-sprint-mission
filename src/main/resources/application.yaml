spring:
    application:
        name: discodeit
    servlet:
        multipart:
            maxFileSize: 10MB
            maxRequestSize: 30MB

discodeit:
    storage:
        type: local
        local:
            root-path: .discodeit/storage
