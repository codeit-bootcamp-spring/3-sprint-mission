spring:
  application:
    name: discodeit

discodeit:
  repository:
    type: file   # jcf | file
    file-directory:
      folder: .discodeit
      user: "/user"
      user-status: "/user-status"
      channel: "/channel"
      message: "/message"
      binary-content: "/binary-content"
      read-status: "/read-status"